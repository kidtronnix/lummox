language: node_js

node_js:
  - 5.0

services:
  - mongodb

script: echo "\n //////// UNIT TESTS" && npm run test-unit && echo '//////// INTEGRATION TESTS' && npm run test-integration && npm run test-lcov
